{% for meta in page.metadata %}
<meta {% if meta.name %}name="{{ meta.name }}" {% endif %}{% if meta.http_equiv %}http-equiv="{{ meta.http_equiv }}" {% endif %}{% if meta.charset %}charset="{{ meta.charset }}" {% endif %}{% if meta.property %}property="{{ meta.property }}" {% endif %}{% if meta.content %}content="{{ meta.content }}" {% endif %}/>
{% endfor %}
<meta property="og:title" content="{% if header.title %}{{ header.title }} | {% endif %}{{ site.title }}" />
<meta property="og:description" content="{{ site.metadata.description }}" />
<meta property="og:url" content="{{ base_url_absolute }}" />
<meta property="og:image" content="{{ base_url_absolute }}{{ theme_url }}/images/og-image.jpg" />
<link rel="icon" sizes="192x192" href="{{ url('theme://images/white-icon.png') }}">
